Generic single-database configuration.

# Run

```
alembic revision --autogenerate -m "add embeddings table"
```

Maybe adjust `SQLALCHEMY_DATABASE_URL` in `sql_app/database.py` when running alembic outside Docker container.